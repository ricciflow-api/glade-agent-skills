{
  "name": "glade-agent-skills",
  "version": "1.0.0",
  "private": true,
  "description": "Agent skills for public Amazon marketplace data through Glade API",
  "license": "MIT",
  "scripts": {
    "test": "python3 scripts/validate_skill.py && python3 -m unittest discover -s skills/amazon-data/tests -v",
    "test:live": "python3 scripts/verify_live.py"
  }
}
